#include "main.h"
/**
 * mul - Multiplies two integers
 * @n: The first number to multiply
 * @m: The second number to multiply with first number
 * Return: The result of multiplication between n param and m param
 */
int mul(int n, int m)
{
	return (n * m);
}
